<!DOCTYPE html>
<html>
  <head>
    <title>Lab7</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.min.js" integrity="sha384-IDwe1+LCz02ROU9k972gdyvl+AESN10+x7tBKgc9I5HFtuNz0wWnPclzo6p9vxnk" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>
  </head>

  <body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container-fluid d-flex align-items-center justify-content-center">
      <h6 class="navbar-brand m-e-5"><%= t('header.logo') %></h6>

      <div class="navbar-nav">
        <%= link_to t('header.home'), home_path, class: "nav-link" %>
        <%= link_to t('header.teachers'), teachers_path, class: "nav-link" %>
        <%= link_to t('header.students'), students_path, class: "nav-link" %>
      </div>

      <div class="ms-2 nav-link dropdown float-end text-white">
        <%= link_to '#', class: 'nav-link dropdown-toggle', data: { "bs-toggle": "dropdown" } do %>
          <%= tag.div '', class: "flag #{I18n.locale}-flag" %>
          <%= t I18n.locale %>
        <% end %>
        <ul class="dropdown-menu">
          <% I18n.available_locales.each do |locale| %>
            <li>
              <% if I18n.locale == locale %>
                <%= tag.span t(locale), class: "dropdown-item" %>
              <% else %>
                <%= link_to t(locale), home_path(locale: locale), class: "dropdown-item" %>
              <% end %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
  <div class="page-content">
    <%= yield %>
  </div>
  </body>
</html>
